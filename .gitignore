# ---------------------------------------------------------------- 原始语料
# corpus/kanripo/ 是 Kanripo 项目的原始 txt。仓库里**没有**随附
# README/LICENSE/版权声明，只有文件头里的 org 元数据（ID / BASEEDITION / WITNESS），
# 无法据此确认可否再分发 —— 按任务书 §23，「如果无法确认：不要上传原始 corpus」。
# 需要语料的人请自行从 Kanripo 获取，再按 README「数据来源与再分发」一节放回原位。
corpus/kanripo/

# ---------------------------------------------------------------- 派生产物
# 数据库、日志、缓存都能由「原始语料 + scripts/pipeline」一键重建，
# 体积又大（data/ 实测 314MB），不适合入库（§23）。
src/data/
src/database/*.db
*.db
*.sqlite
*.sqlite3
*.log

# ---------------------------------------------------------------- 凭据（绝不能入库）
# 本项目零第三方依赖、也不需要任何凭据；这些条目是防线，不是说明。
.env
.env.*
*.pem
*.key

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# ---------------------------------------------------------------- 缓存与日志
# 目前只有 HistoryAI/data/logs（已被 HistoryAI/data/ 覆盖），
# 裸写是防止将来在别处生成时漏网。
logs/
cache/

# ---------------------------------------------------------------- 发布产物
# CI 与本地都用 `python -m scripts.site.build_artifact _site` 装出发布产物，
# 再交给 GitHub Pages。它整体是 HistoryAI/frontend/ 的副本，属于派生物，
# 不入库 —— 入库的话，演示数据就有两份，早晚会不同步。
_site/

# ---------------------------------------------------------------- 编辑器/系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
